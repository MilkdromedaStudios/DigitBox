export function auditHtml(html){const issues=[];const text=String(html||'');if(!/<!doctype html>/i.test(text))issues.push(issue('warning','Missing doctype','Add <!doctype html> for standards mode.'));if(!/<html[\s>]/i.test(text)||!/<\/html>/i.test(text))issues.push(issue('error','Incomplete document','The file must contain a complete <html> document.'));const doc=new DOMParser().parseFromString(text,'text/html');if(!doc.querySelector('meta[name="viewport"]'))issues.push(issue('warning','Missing mobile viewport','Telegram Mini Apps should define a mobile viewport meta tag.'));if(!text.includes('telegram.org/js/telegram-web-app.js'))issues.push(issue('warning','Telegram SDK not loaded','Load the official Telegram Web App bridge when Telegram APIs are used.'));if(!/Telegram\.WebApp\.ready\s*\(|\.ready\s*\(\s*\)/.test(text))issues.push(issue('info','No Telegram ready() call detected','Calling Telegram.WebApp.ready() tells Telegram the Mini App is ready to display.'));if(/\beval\s*\(|new\s+Function\s*\(/.test(text))issues.push(issue('error','Unsafe dynamic JavaScript','Avoid eval() and new Function() in generated Mini Apps.'));if(/\b\d{7,12}:[A-Za-z0-9_-]{25,}\b/.test(text))issues.push(issue('error','Possible Telegram bot token exposed','Remove bot tokens from frontend HTML.'));if(/(sk-[A-Za-z0-9_-]{20,}|AIza[0-9A-Za-z_-]{30,})/.test(text))issues.push(issue('error','Possible API key exposed','Private provider keys must not be embedded in a public Mini App.'));const externalScripts=[...doc.querySelectorAll('script[src]')].map(x=>x.src).filter(Boolean);if(externalScripts.length>6)issues.push(issue('warning','Many external scripts',`${externalScripts.length} external scripts may slow Mini App startup.`));const imagesWithoutAlt=[...doc.querySelectorAll('img')].filter(img=>!img.hasAttribute('alt')).length;if(imagesWithoutAlt)issues.push(issue('info','Images missing alt text',`${imagesWithoutAlt} image${imagesWithoutAlt===1?'':'s'} do not have alt text.`));return issues}
export function preparePreview(html,{visualEdit=false}={}){const bridge=`<script data-appgpt-bridge>(function(){const send=(payload)=>parent.postMessage(Object.assign({source:'appgpt-preview'},payload),'*');const originalError=console.error.bind(console);console.error=function(){try{send({type:'runtime-error',message:[...arguments].map(String).join(' ')})}catch{};return originalError(...arguments)};window.addEventListener('error',e=>send({type:'runtime-error',message:e.message||'Runtime error',line:e.lineno||0,col:e.colno||0}));window.addEventListener('unhandledrejection',e=>send({type:'runtime-error',message:String(e.reason?.message||e.reason||'Unhandled promise rejection')}));function path(el){const parts=[];let node=el;while(node&&node.nodeType===1&&node!==document.documentElement){const tag=node.tagName.toLowerCase(),parent=node.parentElement;if(!parent){parts.unshift(tag);break}const siblings=[...parent.children].filter(x=>x.tagName===node.tagName),suffix=siblings.length>1?':nth-of-type('+(siblings.indexOf(node)+1)+')':'';parts.unshift(tag+suffix);node=parent}return 'html > '+parts.join(' > ')}${visualEdit?`document.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();const el=e.target,cs=getComputedStyle(el);send({type:'visual-select',path:path(el),tag:el.tagName.toLowerCase(),text:el.children.length?'':(el.textContent||'').trim().slice(0,500),styles:{color:cs.color,backgroundColor:cs.backgroundColor,fontSize:cs.fontSize,borderRadius:cs.borderRadius,padding:cs.padding}})},true);document.documentElement.style.cursor='crosshair';`:''}send({type:'preview-ready'});})();<\/script>`;if(/<\/body>/i.test(html))return html.replace(/<\/body>/i,`${bridge}</body>`);return`${html}${bridge}`}
export function applyVisualPatch(html,selector,patch){if(!selector)throw new Error('Select an element in the preview first.');const doc=new DOMParser().parseFromString(html,'text/html'),el=doc.querySelector(selector);if(!el)throw new Error('That element could not be found in the current HTML. Re-select it in the preview.');if(patch.text!==undefined&&patch.text!==''&&el.children.length===0)el.textContent=patch.text;for(const key of['color','backgroundColor','fontSize','borderRadius','padding'])if(patch[key]!==undefined&&patch[key]!=='')el.style[key]=patch[key];return'<!doctype html>\n'+doc.documentElement.outerHTML}
function issue(severity,title,detail){return{id:crypto.randomUUID?.()||String(Math.random()),severity,title,detail}}
