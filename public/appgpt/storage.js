const DB_NAME='AppGPTDB';const DB_VERSION=2;const CHAT_STORE='chats';const TEMPLATE_STORE='templates';const LAST_CHAT_KEY='appgpt_last_chat';const API_KEY_KEY='appgpt_provider_key';const GH_TOKEN_KEY='appgpt_github_token';const tg=window.Telegram?.WebApp;const supportsV9=!!tg?.isVersionAtLeast?.('9.0');
function openDb(){return new Promise((resolve,reject)=>{if(!indexedDB)return reject(new Error('IndexedDB unavailable'));const req=indexedDB.open(DB_NAME,DB_VERSION);req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains(CHAT_STORE)){const chats=db.createObjectStore(CHAT_STORE,{keyPath:'id'});chats.createIndex('updatedAt','updatedAt');}if(!db.objectStoreNames.contains(TEMPLATE_STORE)){const templates=db.createObjectStore(TEMPLATE_STORE,{keyPath:'id'});templates.createIndex('updatedAt','updatedAt');}};req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error||new Error('Could not open local database'));});}
async function put(storeName,value){const db=await openDb();return new Promise((resolve,reject)=>{const tx=db.transaction(storeName,'readwrite');tx.objectStore(storeName).put(clone(value));tx.oncomplete=()=>{db.close();resolve(value)};tx.onerror=()=>{db.close();reject(tx.error)}})}
async function get(storeName,id){const db=await openDb();return new Promise((resolve,reject)=>{const req=db.transaction(storeName,'readonly').objectStore(storeName).get(id);req.onsuccess=()=>{const value=req.result||null;db.close();resolve(value)};req.onerror=()=>{db.close();reject(req.error)}})}
async function getAll(storeName){const db=await openDb();return new Promise((resolve,reject)=>{const req=db.transaction(storeName,'readonly').objectStore(storeName).getAll();req.onsuccess=()=>{const value=req.result||[];db.close();resolve(value)};req.onerror=()=>{db.close();reject(req.error)}})}
async function remove(storeName,id){const db=await openDb();return new Promise((resolve,reject)=>{const tx=db.transaction(storeName,'readwrite');tx.objectStore(storeName).delete(id);tx.oncomplete=()=>{db.close();resolve()};tx.onerror=()=>{db.close();reject(tx.error)}})}
export async function saveChat(chat){await put(CHAT_STORE,chat);await setLastChatId(chat.id);return chat}export const getChat=id=>id?get(CHAT_STORE,id):Promise.resolve(null);export async function listChats(){const rows=await getAll(CHAT_STORE);return rows.sort((a,b)=>new Date(b.updatedAt)-new Date(a.updatedAt))}export async function deleteChat(id){await remove(CHAT_STORE,id);if(await getLastChatId()===id)await setLastChatId('')}
export const saveCustomTemplate=template=>put(TEMPLATE_STORE,template);export const getCustomTemplate=id=>get(TEMPLATE_STORE,id);export async function listCustomTemplates(){const rows=await getAll(TEMPLATE_STORE);return rows.sort((a,b)=>new Date(b.updatedAt)-new Date(a.updatedAt))}export const deleteCustomTemplate=id=>remove(TEMPLATE_STORE,id);
export async function setLastChatId(id){localStorage.setItem(LAST_CHAT_KEY,id||'');if(supportsV9&&tg?.DeviceStorage?.setItem){try{await tgDevice('setItem',LAST_CHAT_KEY,id||'')}catch{}}}export async function getLastChatId(){if(supportsV9&&tg?.DeviceStorage?.getItem){try{const value=await tgDevice('getItem',LAST_CHAT_KEY);if(value)return value}catch{}}return localStorage.getItem(LAST_CHAT_KEY)||''}
export async function saveApiKey(key,remember){sessionStorage.setItem(API_KEY_KEY,key||'');if(!remember){localStorage.removeItem(API_KEY_KEY);if(supportsV9&&tg?.SecureStorage?.removeItem)try{await tgSecure('removeItem',API_KEY_KEY)}catch{}return}if(supportsV9&&tg?.SecureStorage?.setItem){try{await tgSecure('setItem',API_KEY_KEY,key||'');localStorage.removeItem(API_KEY_KEY);return}catch{}}localStorage.setItem(API_KEY_KEY,key||'')}
export async function loadApiKey(){const session=sessionStorage.getItem(API_KEY_KEY);if(session)return{key:session,remembered:false};if(supportsV9&&tg?.SecureStorage?.getItem){try{const value=await tgSecure('getItem',API_KEY_KEY);if(value)return{key:value,remembered:true}}catch{}}const local=localStorage.getItem(API_KEY_KEY)||'';return{key:local,remembered:Boolean(local)}}
export function saveGithubToken(token){sessionStorage.setItem(GH_TOKEN_KEY,token||'')}export function loadGithubToken(){return sessionStorage.getItem(GH_TOKEN_KEY)||''}export function clearGithubToken(){sessionStorage.removeItem(GH_TOKEN_KEY)}
function tgDevice(method,...args){return new Promise((resolve,reject)=>{try{tg.DeviceStorage[method](...args,(err,value)=>err?reject(new Error(String(err))):resolve(value))}catch(e){reject(e)}})}function tgSecure(method,...args){return new Promise((resolve,reject)=>{try{tg.SecureStorage[method](...args,(err,value)=>err?reject(new Error(String(err))):resolve(value))}catch(e){reject(e)}})}function clone(value){try{return structuredClone(value)}catch{return JSON.parse(JSON.stringify(value))}}
